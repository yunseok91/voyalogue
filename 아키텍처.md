# Voyalogue 아키텍처 문서
> 마지막 업데이트: 2026-05-18 | Next.js 15 + Firebase + Vercel

---

## 화면 흐름도

```
랜딩 (/)
  └─ 비로그인 시 노출 → 로그인 유도
  └─ 로그인 상태면 /trips 로 자동 리다이렉트

로그인 (/auth)
  └─ 이메일/비밀번호 또는 Google OAuth

내 여행 목록 (/trips)                 ← 로그인 필수
  ├─ 새 여행 만들기 (/trips/new)
  ├─ 여행 플래너   (/trips/[tripId])
  │    ├─ 비행기/숙소 고정 일정
  │    ├─ 날짜별 장소 추가/수정/삭제 (드래그앤드롭)
  │    ├─ 구글 지도 연동
  │    ├─ 예산 관리 (실시간 환율)
  │    ├─ 체크리스트
  │    ├─ 멤버 초대 → /invite/[code]
  │    └─ 공유 링크 → /share/[code]
  └─ 여행 요약   (/trips/[tripId]/summary)

여행 멤버 화면 (/share/[code])         ← 로그인 불필요 (초대 코드로 접근)
  └─ 편집 코드: 일정 추가/삭제/수정 가능
  └─ 조회 코드: 읽기 전용

초대 수락 (/invite/[code])             ← 멤버 등록 처리 후 /share/[code] 이동

프로필 (/profile)                      ← 로그인 필수
  └─ 이름 변경, 사진 업로드, 아이콘 색상, 통화 설정, 로그아웃, 탈퇴

정적 정보 페이지
  ├─ /contact   → 고객 지원 및 피드백
  ├─ /terms     → 이용약관
  └─ /privacy   → 개인정보처리방침

결제 (미사용 — 코드만 준비)
  ├─ /payment/success
  └─ /payment/fail

관리자 (/admin)                        ← 관리자 계정 전용
  ├─ /admin/users
  ├─ /admin/reports
  └─ /admin/announcements
```

---

## 기술 스택 (실제 버전)

| 영역 | 기술 | 버전 |
|------|------|------|
| 프레임워크 | Next.js (App Router) | 15.1.0 |
| UI | React | 18.3.0 |
| 언어 | TypeScript | 5.8.2 |
| 스타일 | Tailwind CSS | 4.1.14 |
| 전역 상태 | Zustand | 5.0.0 |
| 서버 상태 | TanStack React Query | 5.62.0 |
| 백엔드/DB | Firebase (Auth + Firestore + Storage) | 11.0.0 |
| 지도 | Google Maps API (Places, Autocomplete, Marker) | — |
| 드래그앤드롭 | @dnd-kit (core + sortable + modifiers) | 6.3.x |
| 아이콘 | lucide-react | 0.546.0 |
| 애니메이션 | Motion (Framer Motion v12) | 12.x |
| AI | @google/genai (Gemini) | 1.29.0 |
| 엑셀 내보내기 | xlsx | 0.18.5 |
| 배포 | Vercel | — |
| 광고 | Google AdSense | ca-pub-6889911728160635 |
| 분석 | Google Analytics 4 | G-F8P1XF7MT1 |

---

## 폴더 구조 (실제)

```
voyalogue/
│
├── app/                                  # Next.js App Router 페이지
│   ├── layout.tsx                        # 루트 레이아웃
│   │                                     #  → GoogleAnalytics, InAppBrowserGate,
│   │                                     #    AuthProvider, AdSense 스크립트 삽입
│   ├── globals.css                       # Tailwind v4 전역 스타일
│   ├── global-error.tsx                  # 청크 로딩 실패 등 전역 에러 → 자동 새로고침
│   │
│   ├── page.tsx                          # [화면] 랜딩 /
│   │                                     #  → 히어로, 기능 소개, AdSense 광고 2개
│   │                                     #  → 로그인 상태면 /trips 리다이렉트
│   │
│   ├── auth/page.tsx                     # [화면] 로그인/회원가입 /auth
│   │                                     #  → 이메일+비밀번호, Google OAuth
│   │                                     #  → 베타 한도 초과 시 진입 차단
│   │
│   ├── trips/
│   │   ├── page.tsx                      # [화면] 내 여행 목록 /trips  (760줄)
│   │   │                                 #  → 카드 그리드 (그라디언트 커버)
│   │   │                                 #  → 텍스트 색상 토글 (textDark 플래그)
│   │   │                                 #  → 국가별 필터, 날짜 정렬
│   │   │                                 #  → 엑셀 내보내기 (ExcelModal)
│   │   │
│   │   ├── new/page.tsx                  # [화면] 새 여행 만들기 /trips/new
│   │   │                                 #  → 도시/국가 검색 (RestCountries API + Google Places)
│   │   │                                 #  → 여행 기간, 인원, 테마 컬러 설정
│   │   │                                 #  → 당일치기(0박 1일) 지원
│   │   │                                 #  → 생성 시 shareIndex에 viewCode/editCode 등록
│   │   │
│   │   └── [tripId]/
│   │       ├── page.tsx                  # [화면] 플래너 메인 /trips/[tripId]  (3478줄)
│   │       │                             #  ┌─ 상단 Navbar
│   │       │                             #  │   → 여행 제목/도시, 날짜, 멤버 아바타
│   │       │                             #  │   → 알림 벨 (NotificationBell)
│   │       │                             #  │   → 여행 정보 편집 (날짜/인원/통화 변경)
│   │       │                             #  ├─ 고정 일정 (FixedScheduleSection)
│   │       │                             #  │   → 비행기(입국/출국), 숙소(체크인/아웃)
│   │       │                             #  ├─ 날짜 탭 (Day1~DayN)
│   │       │                             #  │   → 오늘 표시 dot, 일별 지출 합계
│   │       │                             #  ├─ 일정 목록 (드래그앤드롭 SortableItemRow)
│   │       │                             #  │   → 카테고리(식사/장소/쇼핑/교통/기타)
│   │       │                             #  │   → 시간대(아침/점심/저녁/미정)
│   │       │                             #  │   → 별점 투표(멤버별 개인 ratings)
│   │       │                             #  │   → 영수증 첨부(최대 3장, Storage 저장)
│   │       │                             #  │   → 참석 인원 선택
│   │       │                             #  ├─ 예산 푸터
│   │       │                             #  │   → KRW 환산, 예산 진행바
│   │       │                             #  │   → 실시간/고정 환율 전환
│   │       │                             #  │   → 1인 평균 · 명세(정산 팝업)
│   │       │                             #  ├─ 구글 지도 패널 (데스크톱 우측 분할)
│   │       │                             #  ├─ 체크리스트 드로어
│   │       │                             #  └─ 멤버 관리 드로어
│   │       │                             #      → 초대 링크(view/edit 코드) 복사
│   │       │                             #      → 멤버 역할(owner/treasurer/member)
│   │       │
│   │       └── summary/page.tsx          # [화면] 여행 요약 /trips/[tripId]/summary
│   │                                     #  → 별점, 메모 작성
│   │
│   ├── share/[code]/page.tsx             # [화면] 여행 멤버 화면 /share/[code]  (1811줄)
│   │                                     #  → shareIndex에서 uid/tripId 조회
│   │                                     #  → canEdit=true: 일정 추가/수정/삭제 가능
│   │                                     #  → canEdit=false: 읽기 전용
│   │                                     #  → 비로그인도 조회 가능 (게이트 화면)
│   │                                     #  → 플래너와 동일한 레이아웃 (통일화)
│   │                                     #  → AdSense 광고 1개 (멤버 스트립 아래)
│   │
│   ├── invite/[code]/page.tsx            # [화면] 초대 수락 /invite/[code]
│   │                                     #  → memberInvites/{code} 조회
│   │                                     #  → 수락 시 trip.members에 uid 추가
│   │                                     #  → /share/[editCode] 로 이동
│   │
│   ├── profile/page.tsx                  # [화면] 프로필 /profile  (555줄)
│   │                                     #  → 아바타(CLAY 팔레트 + 커스텀 hex)
│   │                                     #  → 구글 유저: 색상 = 프로필 사진 테두리 색
│   │                                     #  → 사진 업로드(Storage), 이름 변경
│   │                                     #  → 통화 설정(localStorage), 비밀번호 재설정
│   │                                     #  → 여행 통계(총 여행수/박수/도시수)
│   │                                     #  → 로그아웃, 계정 탈퇴
│   │
│   ├── contact/page.tsx                  # [화면] 고객 지원 /contact  (정적)
│   ├── terms/page.tsx                    # [화면] 이용약관 /terms  (정적)
│   ├── privacy/page.tsx                  # [화면] 개인정보처리방침 /privacy  (정적)
│   │
│   ├── collection/page.tsx               # [화면] 도시 컬렉션 /collection
│   │                                     #  → 방문한 도시 카드 모음
│   │
│   ├── payment/
│   │   ├── success/page.tsx              # [화면] 결제 성공 (미사용, Toss Payments 준비)
│   │   └── fail/page.tsx                 # [화면] 결제 실패 (미사용)
│   │
│   ├── api/payment/confirm/route.ts      # [API] 결제 확인 (미사용, 서버 사이드)
│   │
│   └── admin/                            # [화면] 관리자 전용
│       ├── layout.tsx                    #  → 관리자 권한 체크
│       ├── page.tsx                      #  → 대시보드 (유저수, 트립수 통계)
│       ├── users/page.tsx                #  → 유저 목록, 정지 처리
│       ├── reports/page.tsx              #  → 신고 목록
│       └── announcements/page.tsx        #  → 공지사항 작성/삭제
│
├── components/                           # 공통 UI 컴포넌트
│   ├── AuthProvider.tsx                  # Firebase onAuthStateChanged 리스너
│   │                                     #  → 로그인 시 users/{uid} upsert
│   │                                     #  → 베타 한도 확인, 계정 정지 체크
│   │                                     #  → user.reload() 로 photoURL 최신화
│   │
│   ├── AuthGuard.tsx                     # 비로그인 → /auth 리다이렉트 HOC
│   │
│   ├── AppNavbar.tsx                     # 하단 탭 네비게이션 (trips/collection/profile)
│   │                                     #  → 로고 클릭 → / (랜딩)
│   │
│   ├── PersonAvatar.tsx                  # 멤버 아바타 컴포넌트
│   │                                     #  → CLAY 팔레트 9가지 색상
│   │                                     #  → photoURL 있으면 프로필 사진 표시
│   │                                     #  → ringColor: 구글 유저 테두리 색
│   │                                     #  → imgFailed 시 SVG 폴백 (photoURL 변경 시 재시도)
│   │
│   ├── TripMap.tsx                       # Google Maps 래퍼
│   │                                     #  → AdvancedMarker로 일정 핀 표시
│   │                                     #  → 지도 클릭 → 장소 추가 패널 연동
│   │
│   ├── FixedScheduleSection.tsx          # 비행기/숙소 고정 일정 UI 섹션
│   │                                     #  → 플래너 상단에 항상 표시
│   │                                     #  → FlightItem, AccommodationItem 렌더링
│   │
│   ├── NotificationBell.tsx              # 알림 벨 컴포넌트
│   │                                     #  → users/{uid}/messages 실시간 구독
│   │                                     #  → 읽지 않은 알림 뱃지 표시
│   │
│   ├── GoogleAnalytics.tsx               # GA4 페이지뷰 추적
│   │                                     #  → usePathname으로 SPA 라우트 변경 감지
│   │
│   ├── AdUnit.tsx                        # Google AdSense 광고 단위
│   │                                     #  → slot 미설정 시 렌더링 안 함 (정책 위반 방지)
│   │                                     #  → adFree=true인 유저는 광고 미표시
│   │
│   ├── AdFreeButton.tsx                  # 광고 제거 버튼 (관리자에서 부여)
│   │
│   ├── InAppBrowserGate.tsx              # 카카오/인스타 인앱 브라우저 감지
│   │                                     #  → 외부 브라우저로 열기 안내
│   │
│   ├── ErrorReloadReset.tsx              # 에러 발생 시 새로고침 버튼
│   │
│   └── organisms/
│       └── ExcelModal/index.tsx          # 여행 데이터 엑셀 내보내기 모달
│                                         #  → xlsx 라이브러리로 .xlsx 파일 생성
│
├── features/
│   └── auth/store.ts                     # Zustand 전역 인증 상태
│                                         #  → user, loading, avatarColor,
│                                         #    avatarHexColor, adFree
│
├── hooks/
│   ├── useScrollLock.ts                  # 모달/드로어 열릴 때 body 스크롤 잠금
│   ├── useDebounce.ts                    # 검색 입력 디바운스
│   └── useMediaQuery.ts                  # 반응형 브레이크포인트 감지
│
├── lib/
│   ├── firebase.ts                       # Firebase app 초기화 (auth, db, storage)
│   ├── firestore.ts                      # 컬렉션 레퍼런스 헬퍼
│   ├── googleMaps.ts                     # Google Maps SDK 싱글턴 로더
│   │                                     #  → 중복 로드 방지, Promise 캐싱
│   ├── tripGradient.ts                   # 그라디언트 색상 유틸
│   │                                     #  → THEME_COLORS: 8가지 프리셋
│   │                                     #  → gradientStyle(): CSS linear-gradient 생성
│   │                                     #  → gradientTextColor(): 밝기 기반 텍스트 색 자동 결정
│   │                                     #  → parseGradientHex(): gradient → {from, to}
│   ├── tripNotification.ts               # 여행 멤버 알림 발송
│   │                                     #  → users/{uid}/messages 에 배치 write
│   │                                     #  → Firebase UID(28자↑)만 발송, 초대코드(6자) 제외
│   ├── inviteCode.ts                     # 초대/공유 코드 생성 (nanoid 기반)
│   ├── currencyMap.ts                    # 통화 기호/이름 매핑, 환율 감지
│   └── exchangeRate.ts                   # 실시간 환율 API 호출 및 KRW 변환
│
├── types/
│   └── gtag.d.ts                         # window.gtag, window.dataLayer 타입 선언
│
├── public/
│   └── ads.txt                           # AdSense 도메인 인증 파일
│
├── .env.local                            # 환경 변수 (아래 참조)
├── next.config.ts                        # Next.js 설정
└── package.json
```

---

## Firestore 데이터 구조 (실제)

```
users/{uid}
  → displayName, email, photoURL, lastLoginAt
  → avatarColor (number), avatarHexColor (string)  ← 아바타 색상
  → adFree (boolean)                                ← 광고 제거 여부
  → suspended (boolean)                             ← 계정 정지 여부

users/{uid}/trips/{tripId}
  → city, country, title
  → startDate, endDate, nights, days
  → gradient ("#from,#to" 형식, 예: "#10B981,#059669")
  → textDark (boolean)  ← 카드 텍스트 색 수동 설정 (share 페이지에도 적용됨)
  → people (인원수)
  → budget (예산)
  → members: [{
      id,           ← 오너: Firebase UID, 초대 멤버: 6자 코드 or Firebase UID
      name,
      role,         ← 'owner' | 'treasurer' | 'member'
      photoURL?,
      colorIndex?,
      hexColor?,
      inviteCode?,
      left?         ← 탈퇴 여부
    }]
  → viewCode   ← /share/[viewCode] → 읽기 전용
  → editCode   ← /share/[editCode] → 편집 가능
  → flights?:    [{ id, name, type, dayId, departTime, arriveTime, lat?, lng? }]
  → accommodations?: [{ id, name, checkInDayId, checkInTime, checkOutDayId, checkOutTime, lat?, lng? }]
  → checklist?:  [{ id, label, done }]
  → currency?    ← 기본 통화 (미설정 시 도시 기반 자동 감지)
  → dayRates?:   { [dayId]: number }  ← 일별 고정 환율
  → createdAt, updatedAt

users/{uid}/trips/{tripId}/days/{dayId}/items/{itemId}
  → name, timeSlot ('아침'|'점심'|'저녁'|'미정')
  → cat ('식사'|'장소'|'쇼핑'|'교통'|'기타')
  → price, currency
  → comment
  → order (드래그 순서)
  → lat, lng
  → rating (레거시, 현재는 ratings 사용)
  → ratings?: { [uid]: number }  ← 멤버별 개인 별점
  → participants (인원수 캐시)
  → participantIds?: string[]    ← 참석 멤버 ID 배열
  → receipts?: string[]          ← Storage URL 배열 (최대 3장)
  → createdAt

users/{uid}/messages/{messageId}
  → title, body, type ('trip')
  → tripPath (클릭 시 이동 경로)
  → read (boolean)
  → createdAt

shareIndex/{code}
  → uid, tripId, canEdit  ← 공유 코드 → trip 경로 역조회용

memberInvites/{code}
  → uid, tripId           ← 초대 코드 → 수락 처리용

config/betaSettings
  → betaEnabled, maxUsers, userCount  ← 베타 가입 한도

announcements/{id}
  → title, body, createdAt
```

---

## 환경 변수 (.env.local)

```bash
# Firebase
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# Google Maps
NEXT_PUBLIC_GOOGLE_MAPS_KEY=

# Google Analytics 4
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Google AdSense
NEXT_PUBLIC_ADSENSE_CLIENT=ca-pub-XXXXXXXXXXXXXXXX
NEXT_PUBLIC_ADSENSE_SLOT=          ← 심사 완료 후 슬롯 ID 입력

# Toss Payments (결제 기능 활성화 시 사용)
NEXT_PUBLIC_TOSS_CLIENT_KEY=
TOSS_SECRET_KEY=
```

---

## 주요 설계 결정 및 주의사항

### 공유 링크 구조
- `viewCode` → `/share/[code]` → `canEdit: false` (읽기 전용)
- `editCode` → `/share/[code]` → `canEdit: true` (편집 가능)
- `shareIndex/{code}` 컬렉션에서 역조회 → 플래너와 멤버 화면이 동일한 Firestore 경로 사용

### 아바타 색상 시스템
- `CLAY` 팔레트: 9가지 색상 (PersonAvatar.tsx에 정의)
- `avatarColor` (숫자 인덱스) 또는 `avatarHexColor` (커스텀 hex) 중 하나 사용
- 구글 유저는 photoURL 표시 + 선택 색상이 `ringColor` (테두리)로 적용

### 플래너 ↔ 멤버 화면 통일화
- 예산 푸터, 날짜 탭, 정산 팝업, 고정 일정 섹션 등 레이아웃을 동일하게 유지
- `gradientTextColor()` 자동 감지 + `trip.textDark` 수동 설정 우선 적용 (두 화면 모두)
- 레이아웃 변경 시 두 파일 모두 수정 필요:
  - `app/trips/[tripId]/page.tsx`
  - `app/share/[code]/page.tsx`

### 알림 시스템
- 일정 추가/수정/삭제, 체크리스트 추가 시 `notifyTripMembers()` 호출
- Firebase UID(28자 이상)만 발송 대상 → 아직 앱에 가입 안 한 초대 코드(6자) 멤버 제외
- `users/{uid}/messages` → `NotificationBell` 컴포넌트가 실시간 구독

### 낙관적 업데이트 (Optimistic Update)
- 아이템 추가 시 `temp_XXXXXX` ID로 즉시 화면 반영
- `addDoc` 완료 후 실제 Firestore ID로 즉시 교체 (`setDayItems`)
- `onSnapshot`이 추가로 동기화 (중복 없이 처리됨)

### AdSense 광고 위치
- `/` 랜딩: 2곳 (trust strip 아래, CTA 위)
- `/share/[code]`: 1곳 (멤버 스트립 아래)
- 로그인 필수 페이지(`/trips/*`, `/profile` 등)에는 광고 없음 (정책 위반 방지)

### Firestore 비용 관리
| 시나리오 | DAU | 일 reads | 상태 |
|--------|-----|---------|------|
| 가입 300명, 20% 활성 | 60명 | ~11,700 | ✅ 안전 |
| 가입 1,000명, 15% 활성 | 150명 | ~29,000 | ✅ 안전 |
| 가입 1,500명, 15% 활성 | 225명 | ~43,800 | ✅ 권장 한도 |
| 가입 2,000명, 15% 활성 | 300명 | ~58,500 | ⚠️ 위험 |

- 1회 접속 기준 약 65 reads (메타 1 + 전체 items 50 + 활성 day 구독 10)
- 일 무료 한도 50,000 reads → 최대 DAU 약 256명

---

## 유지보수 가이드

### 새 화면 추가 시
1. `app/[경로]/page.tsx` 생성
2. 로그인 필수면 `<AuthGuard>` 래핑
3. `AppNavbar`에 탭 추가 여부 검토

### 테마 컬러 추가 시
- `lib/tripGradient.ts` → `THEME_COLORS` 배열에 추가
- `gradientTextColor()` 는 자동으로 적용됨

### 알림 항목 추가 시
- `lib/tripNotification.ts` → `notifyTripMembers()` 호출
- `type: 'trip'` 고정, `tripPath`로 클릭 이동 경로 지정

### 멤버 역할 추가 시
- `app/trips/[tripId]/page.tsx` → `MemberRole` 타입 수정
- `app/share/[code]/page.tsx` → 동일하게 반영
- Firestore 규칙 검토 필요

### 환율 통화 추가 시
- `lib/currencyMap.ts` → `CURRENCY_SYMBOLS`, `CURRENCY_NAMES` 에 추가
- `lib/exchangeRate.ts` → API 지원 여부 확인
- `app/trips/new/page.tsx` → `CURRENCIES` 배열에 추가 (프로필 통화 선택용)


주의필요
정책 위반이 발견되었습니다.

사이트가 애드센스 프로그램 정책을 준수해야 합니다. 위반사항을 수정했으면 사이트 검토를 요청할 수 있습니다.
https://support.google.com/adsense/answer/10502938?hl=ko&visit_id=639148703385475501-2282913790&rd=1
https://support.google.com/publisherpolicies/answer/11036238

게시자 콘텐츠가 없는 화면에 Google 게재 광고
다음과 같은 화면에는 Google 게재 광고가 허용되지 않습니다.
https://support.google.com/publisherpolicies/answer/11112688
콘텐츠가 없거나 가치가 낮은 콘텐츠를 포함한 화면
아직 준비 중인 화면.
알림, 이동 또는 기타 행동 목적으로 사용되는 화면.
게시자 콘텐츠가 없는 화면의 Google 게재 광고에 대해 자세히 알아보기

자세한 내용은 다음 자료를 참고하세요.

고품질 사이트를 만들기 위한 정책 도움말 1부
고품질 사이트를 만들기 위한 정책 도움말 2부
웹마스터 품질 가이드라인
애드센스 프로그램 정책
AdMob 정책 및 제한사항
https://adsense.googleblog.com/2012/04/tips-for-creating-high-quality-sites.html
https://adsense.googleblog.com/2012/09/tips-for-creating-high-quality-sites.html
https://developers.google.com/search/docs/essentials?hl=ko
https://support.google.com/adsense/answer/48182?utm_source=crs&utm_medium=email&utm_campaign=notification
https://support.google.com/admob/answer/6128543


