# 여행 플래너 앱 아키텍처

랜딩(/) → 로그인(/auth) → 내 여행(/trips)
                                │
              ┌─────────────────┼──────────────────┐
              ▼                 ▼                  ▼
       새 여행 만들기         기존 여행 열기       컬렉션
       /trips/new            /trips/:id         /collection
              │                 │
              └──────┬──────────┘
                     ▼
              플래너 메인 /trips/:id
              ├── 지도 + 날짜탭
              ├── 장소 추가/편집 (드래그앤드롭)
              ├── 비행기/숙소 고정 등록
              ├── 예산 관리 (실시간 환율)
              ├── 체크리스트
              ├── 멤버 초대 (/invite/:code)
              └── 여행 공유 (/share/:code)
                     │
              여행 종료 후
                     ▼
              /trips/:id/summary
              (별점·리뷰 작성)

부가 경로
├── /profile          → 프로필 관리
└── /admin            → 관리자 대시보드
    ├── /admin/users
    ├── /admin/reports
    └── /admin/announcements

## 기술 스택

| 영역 | 기술 |
|------|------|
| Frontend | Next.js 15 + React 18 + TypeScript |
| 라우팅 | Next.js App Router (파일 기반 자동 라우팅) |
| 상태관리 | Zustand (전역) + TanStack React Query (서버 상태) |
| 스타일링 | Tailwind CSS v4 |
| 백엔드/DB | Firebase (Auth + Firestore + Storage) |
| 지도 | Google Maps API (Places, Geocoder, AdvancedMarker) |
| 드래그앤드롭 | @dnd-kit (core + sortable + modifiers) |
| 애니메이션 | Motion (Framer Motion v12) |
| AI | @google/genai (Gemini) |
| 엑셀 내보내기 | xlsx |

---

## Firestore 데이터 구조

```
users/{uid}/
  profile: { displayName, email, photoURL, seeded }

users/{uid}/trips/{tripId}/
  meta: {
    title,                 // 여행 제목 (선택)
    city, country,
    startDate, endDate,
    nights, days,
    gradient,              // "#from,#to" 형식 (예: "#3B82F6,#1D4ED8")
    people,                // 인원 수
    members: [{            // 멤버 배열
      uid, displayName, email, photoURL, role  // 'owner' | 'editor'
    }],
    inviteCode,            // 초대 링크 코드
    shareCode,             // 공유 링크 코드 (읽기 전용)
    budget,
    totalRating,           // 전체 여행 평점 (1-5)
    createdAt, updatedAt
  }
  days/{dayId}/items/{itemId}: {
    name, timeSlot,        // 아침 | 점심 | 저녁 | 미정
    cat,                   // 식사 | 장소 | 쇼핑 | 교통 | 기타 | 숙소 | 비행기
    price, currency,
    comment,
    order,
    lat, lng,
    rating,                // 장소별 별점 (1-5)
    reviewText,
    photos: string[],
    // 비행기/숙소 전용
    isPinned,              // 일정 상단 고정 여부
    checkIn, checkOut,     // 숙소 체크인/아웃 시간
    departure, arrival,    // 비행기 출발/도착 시간
    flightType             // 'inbound' | 'outbound'
  }
  checklist/{itemId}: { text, done, category }

reports/{reportId}: { reporterUid, reason, targetType, targetId, createdAt }
announcements/{id}: { title, body, createdAt }
```

---

## 폴더 구조 (현재 상태)

```
voyalogue/
│
├── app/                          # Next.js App Router
│   ├── layout.tsx                # 루트 레이아웃 (AuthProvider)
│   ├── globals.css               # Tailwind v4 전역 스타일
│   ├── page.tsx                  # / → 랜딩 (히어로 + 기능 소개)
│   ├── auth/
│   │   └── page.tsx              # /auth → 로그인/회원가입
│   ├── trips/
│   │   ├── page.tsx              # /trips → 내 여행 목록 (대시보드)
│   │   ├── new/
│   │   │   └── page.tsx          # /trips/new → 여행 생성
│   │   └── [tripId]/
│   │       ├── page.tsx          # /trips/:id → 플래너 메인
│   │       └── summary/
│   │           └── page.tsx      # /trips/:id/summary → 요약/평점
│   ├── collection/
│   │   └── page.tsx              # /collection → 도시 컬렉션
│   ├── profile/
│   │   └── page.tsx              # /profile → 프로필
│   ├── invite/
│   │   └── [code]/page.tsx       # /invite/:code → 초대 수락
│   ├── share/
│   │   └── [code]/page.tsx       # /share/:code → 여행 공유 (읽기 전용)
│   └── admin/
│       ├── layout.tsx
│       ├── page.tsx              # /admin → 대시보드
│       ├── users/page.tsx
│       ├── reports/page.tsx
│       └── announcements/page.tsx
│
├── components/                   # 공통 UI 컴포넌트
│   ├── AuthProvider.tsx          # Firebase Auth 컨텍스트
│   ├── AuthGuard.tsx             # 보호된 라우트 가드
│   ├── AppNavbar.tsx             # 상단 네비게이션 바
│   ├── PersonAvatar.tsx          # 멤버 아바타 (클레이 스타일)
│   ├── TripMap.tsx               # Google Maps 래퍼
│   └── organisms/
│       └── ExcelModal/           # 엑셀 내보내기 모달
│
├── features/
│   └── auth/
│       └── store.ts              # Zustand auth 슬라이스
│
├── hooks/                        # 앱 전역 공통 훅
│   ├── useDebounce.ts
│   └── useMediaQuery.ts
│
├── lib/                          # 외부 서비스 초기화 & 헬퍼
│   ├── firebase.ts               # Firebase app, auth, db, storage
│   ├── firestore.ts              # 컬렉션 레퍼런스 헬퍼
│   ├── googleMaps.ts             # Google Maps 로더 싱글턴
│   ├── inviteCode.ts             # 초대/공유 코드 생성
│   ├── tripGradient.ts           # 그라디언트 색상 유틸 + THEME_COLORS
│   ├── currencyMap.ts            # 통화 기호/이름 매핑
│   └── exchangeRate.ts           # 실시간 환율 조회 & 변환
│
├── utils/                        # 순수 유틸
│   ├── currency.ts               # 환율 변환 (레거시)
│   ├── date.ts                   # 날짜 포맷
│   ├── excel.ts                  # xlsx 내보내기 로직
│   ├── dragOrder.ts              # dnd-kit order 재할당 로직
│   └── seed.ts                   # 개발용 시드 데이터 (Firestore)
│
├── types/                        # TypeScript 타입 정의
│   ├── trip.ts
│   ├── planner.ts
│   └── user.ts
│
├── public/                       # 정적 파일
├── .env.local                    # Firebase / Google Maps API 키
├── firestore.rules               # Firestore 보안 규칙
├── storage.rules                 # Storage 보안 규칙
└── package.json
```

---

## 개발 단계

### 1단계 — 기반 구축 ✅
- [x] Next.js 15 + TypeScript 프로젝트 세팅
- [x] Tailwind CSS v4 설정
- [x] TypeScript 타입 정의 (trip.ts, planner.ts, user.ts)
- [x] Firebase 초기화 및 실제 연결 (lib/firebase.ts + .env.local)
- [x] Firestore 컬렉션 레퍼런스 헬퍼 (lib/firestore.ts)
- [x] Zustand auth 슬라이스 (features/auth/store.ts)

### 2단계 — 랜딩 & 인증 ✅
- [x] 랜딩 페이지 (히어로, 기능 소개, 리뷰 섹션)
- [x] 로그인 / 회원가입 (Firebase Auth — 이메일 + Google)
- [x] AuthGuard (보호된 라우트 — /trips, /trips/:id 등)
- [x] AuthProvider (전역 인증 컨텍스트)

### 3단계 — 플래너 핵심 ✅
- [x] 여행 생성 폼 (/trips/new) — 국가 검색, 날짜, 인원, 테마 색상
- [x] 플래너 메인 (/trips/:id) — 드래그앤드롭 일정 (@dnd-kit)
- [x] Google Maps 연동 (TripMap 컴포넌트)
- [x] 별점/리뷰 기능
- [x] 실시간 환율 (lib/exchangeRate.ts + lib/currencyMap.ts)
- [x] 비행기/숙소 고정 등록 (일정 상단 핀)
- [x] 멤버 초대 (inviteCode + /invite/:code)
- [x] 여행 공유 링크 (/share/:code)

### 4단계 — 다중 여행 & 부가 기능 ✅
- [x] 내 여행 목록 대시보드 (/trips)
- [x] 도시 컬렉션 (/collection)
- [x] 여행 요약/평점 (/trips/:id/summary)
- [x] 프로필 페이지 (/profile)
- [x] 엑셀 내보내기 (ExcelModal + utils/excel.ts)
- [x] 관리자 패널 (/admin)

---

## 주요 설계 결정

| 결정 | 이유 |
|------|------|
| Next.js App Router | 파일 기반 라우팅, SSR/SSG 옵션, 업계 표준 |
| Firebase Firestore | 다중 유저/여행 구조에 적합, 실시간 구독 |
| Zustand | Redux 대비 보일러플레이트 없음, 작은 번들 |
| @dnd-kit | 접근성 내장, Next.js/React 18 호환성 우수 |
| gradient "#from,#to" 형식 | Tailwind 클래스 의존 제거, 자유로운 커스텀 색상 |
| inviteCode + shareCode 분리 | 편집 초대(inviteCode) vs 읽기 공유(shareCode) 권한 분리 |

등록 사용자 안전 기준
시나리오	등록 사용자	DAU 추정	일 reads	상태
가입자 300명, 20% 활성	300명	60명	~11,700	✅ 안전
가입자 1,000명, 15% 활성	1,000명	150명	~29,000	✅ 안전
가입자 2,000명, 15% 활성	2,000명	300명	~58,500	⚠️ 위험
가입자 1,500명	1,500명	225명	~43,800	✅ 권장 한도
→ 등록 사용자 1,500명이 실질적인 안전선

앱 1회 접속 시 Firestore 읽기 횟수 계산
현재 코드 기준으로 트립 플래너 한 번 열 때 발생하는 reads:


트립 목록 페이지:
  - 내 여행 N개 조회: N reads (트립 5개 = 5 reads)

트립 플래너 열 때:
  - 메타 문서 1개: 1 read
  - 전체 일정 초기 로드: 5일 × 10개 아이템 = 50 reads
  - 활성 day onSnapshot 초기: 10 reads
  ─────────────────────────────
  1회 접속당 총 약 60~70 reads
일별 무료 한도 역산

무료 읽기 한도:  50,000 reads/일
1회 접속 비용:  약 65 reads
───────────────────────────────
최대 허용 접속:  50,000 ÷ 65 = 769회/일

사용자 1명이 하루 3번 접속 가정:
  769 ÷ 3 = 약 256명 DAU (일 활성 사용자)


1.구글 지도 검색 기능 > 일정 등록 기능
2.사진 아이콘 누르면 편집이됨 > 사진 업로드만 할 수 있도록 수정
3.멤버 > 여행 명세 (정산금액) 버튼이 활성화가 안돼 
4.비행기(입국,출국) 숙소(체크인,체크아웃) 등록하면 두벌씩 등록되는것 같아 확인해줘
5.아침, 점심, 저녁 , 미정 영역간에 드래그앤 드랍으로 이동이 안됨
6.
교통, 기타 레이블 text-gray-600 이게 같은것 같은데 교통 다르게 css 다르겦 표시 초록색? 어울리만한 색상으로 수정


1. 장소검색 아닌 직접 추가도 가능하게 해야될 것 같아
예를 들면 장소검색이 아닌 택시 이용, 교통카드
카테고리엔 교통이란게 있는데 이런 것들을 추가하고 싶을떈 어떤식으로 개선해야할까?

pc 화면에서 지도상에서 더블 클릭하면 마커 표시되어 장소 등록하게 좌표로 할 수 있던지 하게 해줘 모바일화면에서는 더블 터치인데 ui ux 고려해서해주고

여행일지 첫 화면에서 지도 마커표시가 안돼
탭을 이동해야 보여

마커 화살표 이어진걸 빨간색으로 식별 가능하게 --> 이렇게 표시해줘


a ,b ,c 사용자

1일정 a,b 20만원 지출
2일정 a,c 10만원 지출
a 금액 15만원
b 금액 10만원
c 금액 5만원
이렇게 누적되서 계산이 안되는것 같은데 
=============
1. 숙소,공항 마커 표시 아이콘 숙소모양,비행기 모양 개선 
2. 여행 컬렉션 개선 > 방문도시가 아니라 방문 나라로 수정 
나라 별로 그룹화 
나라 카드 클릭시 해당 나라 여행일정만 볼 수 있도록 필터 수정
나라 카드 배경을 해당 국기로 표시되게 개선 
호버 시 삭제 얼럿 띄우고 "정말로 삭제하시겠습니다 복구 불가능" 안내 문구 노출 후 삭제 팔래트 아이콘 표시 제거 
3. 어드민 공지사항 등록 > 내 여행일정 하단이던 상단이던 공지사항 표시 되게 ui ux 고려해서표시 기능 추가 
-------

4. 오류 이게 나는것 같은데 @app/globals.css  